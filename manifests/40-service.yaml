apiVersion: v1
kind: Service
metadata:
  name: kube-crisp-apiserver
  namespace: kube-crisp
  # Selected by the ServiceMonitor in manifests/optional/.
  labels:
    app.kubernetes.io/name: kube-crisp-apiserver
spec:
  selector:
    app.kubernetes.io/name: kube-crisp-apiserver
  ports:
    - name: https
      port: 443
      targetPort: https
